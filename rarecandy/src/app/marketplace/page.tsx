import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Marketplace",
  description: "Browse authenticated Pokémon cards with live market data, grading info, and verified seller listings.",
};

export { default } from "./MarketplaceClient";
